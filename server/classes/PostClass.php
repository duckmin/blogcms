<?php
	/*include_once dirname(__FILE__)."/../configs.php";
	
	class PostClass extends PostFormatter
	{
		
		
		public function getHomePagePosts( $posts_from_db ){
			$str="";
			$L=count( $posts_from_db );
			for( $i=0; $i<$L; $i+=1 ){
				if( $i < $GLOBALS['amount_on_main_page'] ){
					$single = $posts_from_db[$i];
					//echo print_r($single)."<br>";
					$post_html = $this->getPostHTMLFromDBData( $single["folder_path"], $single["id"], $single["tags"], $single["created"], $single["title"] );
					$str.=$post_html;
				}
			}
			return $str;
		}
			
	};*/
	

?>

